name: My first workflow

on: push

job: 
   fisrt_job:
       runs on: ubantu-latest
       steps:
       - name: welcome message
         run: echo "my first github action job"

       - name: list files
         run: ls

       - name: read files
         run: Cat README.md  
